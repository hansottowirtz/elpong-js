/// <reference types="elpong"/>

import * as elpong from 'elpong';

console.log(elpong);

// Elpong.enableAutoload();
// const pig = Elpong.get('test-app').select('pigs').find(3);
// document.write(pig.fields.name);