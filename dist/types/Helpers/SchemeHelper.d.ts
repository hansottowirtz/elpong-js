import { Scheme } from '../Scheme';
import { Collection } from '../Collection';
export declare namespace SchemeHelper {
    function getCollectionBySingularName(scheme: Scheme, singular_name: string): Collection;
}
