import { Element } from '../../../Element';
import { EmbeddedCollectionFieldConfiguration } from '../../../Configuration';
import { PreElement } from '../../../PreElement';
export declare namespace EmbeddedCollection {
    function handle(element: Element, pre_element: PreElement, field_key: string, field_config: EmbeddedCollectionFieldConfiguration): void;
}
